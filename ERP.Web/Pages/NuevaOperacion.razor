@page "/nueva-operacion"
@using Microsoft.AspNetCore.Components.Routing
@* ^ Esta línea es la que soluciona tu error RZ10012 *@

<script src="https://cdn.tailwindcss.com"></script>

<div class="max-w-[1000px] mx-auto p-8 animate-in slide-in-from-bottom duration-500">
    
    @* --- CABECERA --- *@
    <div class="flex items-center justify-between mb-8 border-b border-slate-300 pb-4">
        <div>
            <h1 class="text-3xl font-black text-slate-800 uppercase tracking-tighter">Nueva Operación</h1>
            <p class="text-slate-500 font-medium italic text-sm">Registro de documento comercial / contable</p>
        </div>
        <NavLink href="" class="bg-slate-200 hover:bg-slate-300 text-slate-700 px-4 py-2 rounded font-bold text-xs transition no-underline shadow-sm border border-slate-300">
            VOLVER AL PANEL
        </NavLink>
    </div>

    @* --- FORMULARIO ESTILO FICHA --- *@
    <div class="bg-white rounded-xl shadow-xl border border-slate-200 overflow-hidden">
        <div class="bg-slate-900 p-4 text-white text-xs font-bold uppercase tracking-widest flex justify-between">
            <span>Datos del Documento</span>
            <span class="text-blue-400">ID: BORRADOR</span>
        </div>
        
        <div class="p-8 grid grid-cols-1 md:grid-cols-2 gap-8">
            
            @* Selección de Cliente *@
            <div class="space-y-2">
                <label class="block text-xs font-black text-slate-500 uppercase tracking-wider">Cliente / Entidad</label>
                <select class="w-full bg-slate-50 border-2 border-slate-200 rounded-lg p-3 text-sm focus:border-blue-600 outline-none font-bold text-slate-700 transition">
                    <option>-- Seleccionar de la base de datos --</option>
                    <option>Comercial Arandina S.L.</option>
                    <option>Construcciones Ruiz S.A.</option>
                    <option>Instalaciones Eléctricas Gómez</option>
                </select>
            </div>

            @* Tipo de Operación *@
            <div class="space-y-2">
                <label class="block text-xs font-black text-slate-500 uppercase tracking-wider">Tipo de Registro</label>
                <div class="flex gap-2 h-[46px]">
                    <button class="flex-1 bg-blue-800 text-white rounded-lg font-black text-xs shadow-md uppercase">Factura Emitida</button>
                    <button class="flex-1 bg-slate-100 border border-slate-300 text-slate-500 rounded-lg font-black text-xs hover:bg-slate-200 uppercase">Presupuesto</button>
                </div>
            </div>

            @* Descripción *@
            <div class="md:col-span-2 space-y-2">
                <label class="block text-xs font-black text-slate-500 uppercase tracking-wider">Concepto de la Operación</label>
                <input type="text" placeholder="Ej: Servicios de consultoría técnica febrero 2026..." 
                       class="w-full bg-slate-50 border-2 border-slate-200 rounded-lg p-3 text-sm focus:border-blue-600 outline-none transition" />
            </div>

            @* Importe Principal *@
            <div class="space-y-2">
                <label class="block text-xs font-black text-slate-500 uppercase tracking-wider">Base Imponible (€)</label>
                <div class="relative">
                    <span class="absolute left-4 top-3 font-bold text-slate-400">€</span>
                    <input type="number" step="0.01" placeholder="0.00" 
                           class="w-full bg-slate-50 border-2 border-slate-800 rounded-lg p-3 pl-8 text-2xl font-mono font-bold text-slate-900 focus:bg-white outline-none transition shadow-inner" />
                </div>
            </div>

            @* Configuración de Impuestos *@
            <div class="space-y-2">
                <label class="block text-xs font-black text-slate-500 uppercase tracking-wider">Tipo de IVA Aplicable</label>
                <select class="w-full bg-slate-50 border-2 border-slate-200 rounded-lg p-3 text-sm font-bold text-slate-700">
                    <option>21% IVA General</option>
                    <option>10% IVA Reducido</option>
                    <option>4% IVA Superreducido</option>
                    <option>Exento / Inversión Sujeto Pasivo</option>
                </select>
            </div>

        </div>

        @* --- ACCIONES FINALES --- *@
        <div class="bg-slate-50 p-8 border-t border-slate-200 flex flex-col md:flex-row justify-between items-center gap-4">
            <p class="text-[11px] text-slate-400 font-medium italic max-w-xs text-center md:text-left">
                Al confirmar, el sistema validará el stock (si aplica) y registrará el asiento contable automáticamente.
            </p>
            <button class="w-full md:w-auto bg-green-700 hover:bg-green-800 text-white px-12 py-4 rounded-xl font-black text-sm shadow-xl transition transform active:scale-95 uppercase tracking-widest border-b-4 border-green-900">
                💾 Confirmar y Guardar
            </button>
        </div>
    </div>
</div>

@code {
    // Aquí pondremos la lógica para enviar los datos a la API en el siguiente paso.
}